{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    {{ form_start(formulaireGestionProfil) }}
    <div class="formulaireGestionProfil labelprofil">{{ form_widget(formulaireGestionProfil) }}</div>
    <p id="numeroCampus">Campus: {{ participant.campus }}</p>
    <button class="btn-gestion-profil" type="submit">Enregistrer</button>

    {{ form_end(formulaireGestionProfil) }}
    <br>
    <a href="{{ path('home') }}" target="_blank"> <input type="button" value="Annuler" class="btn-gestion-profil"></a>
{% endblock %}
