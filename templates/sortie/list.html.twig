{%  extends 'base.html.twig' %}

{%  block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-3">
                {%  include 'sortie/_filtre.html.twig' with {form: form } only %}
                <a href="{{ path('sortie_creation') }}" class="btn btn-info w-100">Créer une sortie</a>
            </div>

             <div class="col-md-9">
                 <div class="grid">
                {% for sortie in sorties %}
                    {%  include 'sortie/_card.html.twig' with {sortie: sortie } only %}

                {% endfor %}
                </div>
        </div>
    </div>
{% endblock %}

{%  block title %}
    Toutes les sorties selon votre campus | {{ parent() }}
{%  endblock %}